# Screenshots

Drop the app screenshots here with exactly these filenames — the page picks them up
automatically and the dashed placeholder disappears.

| File | Shown on | Suggestion |
| --- | --- | --- |
| `artistlab-1.png` | ArtistLab, first phone | The palette extracted from a reference photo |
| `artistlab-2.png` | ArtistLab, second phone | Colour Lab (wheel / harmonies) or Paint Lab |
| `mymood-1.png` | MyMood, first phone | The diary cover or the home screen |
| `mymood-2.png` | MyMood, second phone | Mood insight charts, or the calendar |
| `storeflow-1.png` | StoreFlow, first phone | The metrics dashboard |
| `storeflow-2.png` | StoreFlow, second phone | Barcode scanning or voice logging |

## Format

- **PNG**, portrait, taken on an iPhone simulator or device.
- Any modern iPhone resolution works (e.g. 1179×2556 or 1290×2796). The frame crops with
  `object-fit: cover`, so keep the important content away from the extreme edges.
- The page draws its own iPhone bezel and Dynamic Island — send the **raw screen capture**,
  not one already placed inside a device mockup.
- Keep each file under ~600 KB if you can; run them through ImageOptim or TinyPNG before
  committing so the page stays fast.
